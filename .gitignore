# macOS
.DS_Store

# harness runtime cache (citation lookups)
.harness/cache/

# build artifacts (versioned folders with PDFs, logs, manifests)
build/

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz

# local Claude settings (per-user permissions)
.claude/settings.local.json

# Python bytecode
__pycache__/
